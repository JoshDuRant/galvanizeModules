function transformFirstAndLast(arr) {
    let newObj = {};
    newObj [arr[0]] = arr[arr.length-1];
    return newObj;
}

let test = transformFirstAndLast(['Kevin', 'Bacon', 'Love', 'Hart', 'Costner', 'Spacey']);
console.log(test); // should return Kevin: Spacey
