function computeProductOfAllElements(arr) {
  let product = arr[0];
  if (arr.length === 0) {
      return 0;
  }
  for (let i = 1; i < arr.length; i++) {
    product *= arr[i];
  }
  return product;
}
