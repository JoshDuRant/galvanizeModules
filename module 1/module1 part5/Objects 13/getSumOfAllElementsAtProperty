function getSumOfAllElementsAtProperty(obj, key) {
  let ourArray = obj[key];
  if (ourArray === undefined || !Array.isArray(ourArray) || ourArray === []) {
    return 0;
  }
  let sum = 0;
  for (let i = 0; i < ourArray.length; i++) {
    sum += ourArray[i];
  }
  return sum;
}
